# Архитектура кликера - Система State Management

## 📁 Структура проекта

```
lib/
├── main.dart                 # Главная точка входа и навигация
├── models/
│   ├── game_state.dart       # Основное состояние игры
│   └── upgrade.dart          # Модель улучшения
└── screens/
    ├── game_screen.dart      # Экран игры с кликером
    └── shop_screen.dart      # Экран магазина улучшений
```

## 🏗️ Архитектурные решения

### 1. **GameState** - Центральное состояние
- Использует `ChangeNotifier` для уведомления UI об изменениях
- Хранит все данные игры: очки, улучшения, фильтры
- Предоставляет методы для изменения состояния
- Автоматически пересчитывает характеристики при покупке улучшений

### 2. **Upgrade** - Модель улучшения
- Enum `UpgradeType` для типизации улучшений
- Методы для получения описаний и цветов
- Инкапсуляция состояния покупки

### 3. **Фильтры магазина**
Реализованы 3 типа фильтрации:
- **Купленные** - показать только купленные улучшения
- **Доступные** - показать только те, что можно купить
- **Сброс фильтров** - показать все улучшения

## 🎮 Игровая механика

### Типы улучшений:
1. **ClickPower** 🔥 - Увеличивает очки за клик
2. **Passive** 🤖 - Добавляет пассивный доход
3. **Multiplier** ✨ - Умножает все доходы

### Система прогресса:
- Показывает прогресс до следующего доступного улучшения
- Форматирование больших чисел (K, M)
- Визуальная индикация состояния улучшений

## 🎨 UI/UX Особенности

### Цветовая схема:
- **Купленные**: Зелёная рамка и галочка
- **Доступные**: Синяя рамка с подсветкой
- **Недоступные**: Серая рамка

### Анимации:
- Анимация сжатия кнопки при клике
- Вибрация при взаимодействии
- Плавные переходы состояний

### Адаптивность:
- Использование `IndexedStack` для быстрого переключения
- Responsive дизайн карточек улучшений
- Умное форматирование чисел

## 🔧 Как расширить

### Добавить новое улучшение:
```dart
Upgrade(
  id: 'unique_id',
  name: 'Название',
  description: 'Описание эффекта',
  basePrice: 100,
  type: UpgradeType.clickPower, // или passive/multiplier
  value: 5,
  iconData: '🚀',
)
```

### Добавить новый тип фильтра:
1. Добавить boolean поле в `GameState`
2. Добавить метод для переключения
3. Обновить `getFilteredUpgrades()`
4. Добавить UI в `shop_screen.dart`

### Добавить сохранение прогресса:
```dart
// В GameState добавить методы:
void saveToStorage() { /* SharedPreferences */ }
void loadFromStorage() { /* SharedPreferences */ }
```

## 🎯 Преимущества архитектуры

✅ **Модульность** - легко добавлять новые функции  
✅ **Тестируемость** - логика отделена от UI  
✅ **Производительность** - эффективные уведомления  
✅ **Масштабируемость** - простое расширение  
✅ **Читаемость** - понятная структура кода

## 🚀 Быстрый старт для модификаций

1. **Изменить цены**: Отредактировать `basePrice` в `_initializeUpgrades()`
2. **Добавить улучшение**: Добавить новый `Upgrade` в список
3. **Изменить фильтры**: Добавить новые boolean поля и методы
4. **Настроить UI**: Изменить цвета и стили в screen файлах
5. **Добавить звуки**: Использовать пакет `audioplayers`

Этот кликер готов к развитию и легко масштабируется! 🎮